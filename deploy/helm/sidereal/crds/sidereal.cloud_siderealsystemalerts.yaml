---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: siderealsystemalerts.sidereal.cloud
spec:
  group: sidereal.cloud
  names:
    kind: SiderealSystemAlert
    listKind: SiderealSystemAlertList
    plural: siderealsystemalerts
    shortNames:
    - ssa
    singular: siderealsystemalert
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.reason
      name: Reason
      type: string
    - jsonPath: .spec.acknowledged
      name: Acknowledged
      type: boolean
    - jsonPath: .spec.acknowledgedBy
      name: Acknowledged By
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SiderealSystemAlert indicates a degraded state in the Sidereal system.
          Unacknowledged alerts block probe scheduling until an individual principal
          acknowledges and provides a remediation action.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SiderealSystemAlertSpec defines the content of a system degradation
              alert.
            properties:
              acknowledged:
                default: false
                description: Acknowledged indicates whether an individual principal
                  has acknowledged this alert.
                type: boolean
              acknowledgedAt:
                description: AcknowledgedAt is the timestamp of acknowledgment.
                format: date-time
                type: string
              acknowledgedBy:
                description: |-
                  AcknowledgedBy is the Kubernetes username of the principal who acknowledged the alert.
                  Must be an individual user identity, not a shared ServiceAccount.
                type: string
              message:
                description: Message is a human-readable description of the alert
                  condition.
                type: string
              reason:
                description: Reason categorizes the alert.
                enum:
                - AdmissionPolicyMissing
                - SIEMExportDegraded
                - AuditWriteFailure
                - BaselineConfigurationDrift
                - TamperedResult
                - AOAuthorizationExpired
                - BackendUnreachable
                - UnexpectedNetworkFlow
                type: string
              remediationAction:
                description: RemediationAction describes the action taken or planned
                  to remediate the condition.
                type: string
            required:
            - acknowledged
            - message
            - reason
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
