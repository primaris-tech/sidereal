trestle:
  version: "3"
  # Sidereal OSCAL workspace
  # Author in md_component_definitions/, compile with: trestle assemble component-definition -n sidereal
  # Validate compiled output with: oscal-cli component-definition validate -f component-definitions/sidereal/component-definition.json
