---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: siderealaoauthorizations.sidereal.cloud
spec:
  group: sidereal.cloud
  names:
    kind: SiderealAOAuthorization
    listKind: SiderealAOAuthorizationList
    plural: siderealaoauthorizations
    shortNames:
    - sao
    singular: siderealaoauthorization
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.aoName
      name: AO
      type: string
    - jsonPath: .status.active
      name: Active
      type: boolean
    - jsonPath: .spec.expiresAt
      name: Expires
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SiderealAOAuthorization is a time-bounded, technique-scoped, namespace-scoped
          authorization from an Authorizing Official for detection probe execution.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SiderealAOAuthorizationSpec defines a time-bounded authorization
              for detection probes.
            properties:
              aoName:
                description: AOName is the name of the Authorizing Official (individual,
                  not a role or team).
                minLength: 1
                type: string
              authorizedNamespaces:
                description: |-
                  AuthorizedNamespaces is the list of namespaces where detection probes may execute.
                  No wildcards allowed.
                items:
                  type: string
                minItems: 1
                type: array
              authorizedTechniques:
                description: AuthorizedTechniques is the list of MITRE ATT&CK technique
                  IDs authorized for probing.
                items:
                  type: string
                minItems: 1
                type: array
              catalogVersion:
                description: CatalogVersion is the approved syscall catalog version
                  reference.
                type: string
              expiresAt:
                description: ExpiresAt is the end of the authorization window.
                format: date-time
                type: string
              justification:
                description: Justification is the stated reason for the authorization.
                minLength: 1
                type: string
              validFrom:
                description: ValidFrom is the start of the authorization window.
                format: date-time
                type: string
            required:
            - aoName
            - authorizedNamespaces
            - authorizedTechniques
            - expiresAt
            - justification
            - validFrom
            type: object
          status:
            description: SiderealAOAuthorizationStatus defines the observed state
              of the authorization.
            properties:
              active:
                description: Active is computed from the time bounds (validFrom <=
                  now < expiresAt).
                type: boolean
            required:
            - active
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
